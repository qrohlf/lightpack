import React from 'react'
import styles from './LighterpackImportPage.module.css'

export const LighterpackImportPage = ({ children }) => {
  return (
    <div className={styles.LighterpackImportPage}>
      TODO - import from lighterpack and then redirect to the newly created pack
      DB entry
      {children}
    </div>
  )
}
